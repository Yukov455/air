# Stock Analytics Monitoring System

Система автоматического обновления данных и переобучения ML модели для анализа финансовых рынков.

## 🚀 Быстрый старт

### Docker Compose (рекомендуется)

```bash
cd monitoring_system
docker-compose up -d
```

Дашборд будет доступен по адресу: http://localhost:8080

### Локальный запуск

```bash
cd monitoring_system
pip install -r requirements.txt
python app.py
```

## 📊 Возможности

- **Автоматическое обновление котировок** - каждые 5 минут
- **Автоматическое переобучение модели** - каждый час
- **Множество источников данных** с автоматическим fallback:
  - Yahoo Finance (основной)
  - Alpha Vantage (резервный)
  - Finnhub (резервный)
  - Synthetic (если все недоступны)
- **Веб-дашборд** для мониторинга в реальном времени
- **REST API** для управления

## ⚙️ Конфигурация

Переменные окружения:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PORT` | Порт сервера | 8080 |
| `DATA_UPDATE_INTERVAL` | Интервал обновления данных (сек) | 300 |
| `MODEL_TRAIN_INTERVAL` | Интервал переобучения модели (сек) | 3600 |
| `AUTO_START_SCHEDULER` | Автозапуск планировщика | true |
| `ALPHAVANTAGE_API_KEY` | API ключ Alpha Vantage | R89IMMWUAE7HUOCY |
| `FINNHUB_API_KEY` | API ключ Finnhub | demo |

## 📁 Структура проекта

```
monitoring_system/
├── app.py                 # Главное приложение FastAPI
├── Dockerfile            # Docker образ
├── docker-compose.yml    # Docker Compose конфигурация
├── requirements.txt      # Python зависимости
├── services/
│   ├── data_updater.py   # Сервис обновления данных
│   ├── model_trainer.py  # Сервис обучения модели
│   └── scheduler.py      # Планировщик задач
├── webapp/
│   └── dashboard.html    # Веб-дашборд
├── config/
│   └── settings.py       # Конфигурация
├── data/                 # БД (создаётся автоматически)
├── models/               # Модели (создаётся автоматически)
└── logs/                 # Логи (создаётся автоматически)
```

## 🔌 API Endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Дашборд |
| `/health` | GET | Health check |
| `/api/status` | GET | Статус системы |
| `/api/data_stats` | GET | Статистика данных |
| `/api/model_stats` | GET | Статистика модели |
| `/api/training_history` | GET | История обучения |
| `/api/start` | GET | Запустить планировщик |
| `/api/stop` | GET | Остановить планировщик |
| `/api/run_task/{name}` | GET | Запустить задачу вручную |
| `/api/update_data` | POST | Обновить данные |
| `/api/train_model` | POST | Обучить модель |

## 📈 Отслеживаемые тикеры

AAPL, MSFT, GOOGL, AMZN, NVDA, META, TSLA, JPM, V, JNJ

## 🔧 Управление

### Запуск/остановка планировщика

```bash
# Запустить
curl http://localhost:8080/api/start

# Остановить
curl http://localhost:8080/api/stop
```

### Ручной запуск задач

```bash
# Обновить данные
curl http://localhost:8080/api/run_task/data_update

# Обучить модель
curl http://localhost:8080/api/run_task/model_training
```

## 📊 Метрики модели

Универсальная модель использует:
- **RandomForest** + **GradientBoosting** ensemble
- 33 технических индикатора
- Автоматическое сохранение лучшей версии
- Cross-validation для оценки качества

## 🐳 Docker команды

```bash
# Сборка и запуск
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Перезапуск
docker-compose restart
```

## 📝 Лицензия

MIT
